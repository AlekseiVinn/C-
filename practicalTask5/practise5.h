void fillArray(int*, const int);

void printArray(const int*, const int);

int sumArray(int*, const int);

double averageArray(int*, const int);

int sumPosArray(int*, const int);

int sumNegArray(int*, const int);

int sumEvenIndArray(int*, const int);

int sumOddIndArray(int*, const int);

int minIndArray(int*, const int);

int maxIndArray(int*, const int);

long multMinMaxArray(int*, const int);

void sortArray(int*, const int);

bool from_min(const int, const int);

bool from_max(const int, const int);

void bubble_sort(int[], const int, bool (compare)(int,int));

int* max_vector(const int, const int*, const int*);

int searchTranspose(const int len, int* arr, int searchKey);

int v_func(int num1, int num2, int (*some_func)(int a, int b));

int some_sum(int a, int b);

int some_mult(int a, int b);

void mainProcessing(int argc, char** argv);
